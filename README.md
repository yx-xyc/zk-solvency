# ZK Solvency Protocol

A zero-knowledge proof system for cryptocurrency exchange solvency. An exchange proves `total_assets ≥ total_liabilities` without revealing individual user balances. Users can verify their balance is included via a Merkle proof — entirely in the browser.

## How It Works

1. Exchange provides user balances (liabilities) and reserve balances (assets) as private inputs to the SP1 zkVM program.
2. The program builds a SHA-256 Merkle tree over user balances, sums both sides, asserts solvency, and commits four public values: `merkleRoot`, `assetsCommitment`, `totalLiabilities`, `totalAssets`.
3. A ZK proof is generated via the Succinct Prover Network and submitted to `SolvencyAttestation.sol` on-chain.
4. Any user can verify their balance is in the Merkle root using the web UI — no Rust binary required.

## Tech Stack

| Layer | Technology |
|-------|------------|
| zkVM | SP1 v6.1.0 (Succinct) |
| ZK program | Rust (compiled to RISC-V ELF) |
| Proof system | PLONK (Gnark BN254) |
| Smart contract | Solidity + Foundry |
| Frontend | Next.js (TypeScript) |

---

## Architecture

```
Private Inputs              SP1 zkVM Program                 Public Outputs (128 bytes)
──────────────              ─────────────────                ──────────────────────────
User balances (N)  ──────►  Build SHA-256 Merkle tree   ──►  merkleRoot       (bytes32)
Reserve balances   ──────►  Compute assets commitment   ──►  assetsCommitment (bytes32)
                            Sum liabilities & assets    ──►  totalLiabilities (uint64)
                            Assert assets ≥ liabilities ──►  totalAssets      (uint64)
                                        │
                                        ▼
                               ZK Proof (PLONK)
                                        │
                                        ▼
                            SolvencyAttestation.sol
                            Verifies proof on-chain via SP1 PLONK gateway
                            Emits SolvencyProven event, stores attestation
```

---

## Live Deployment (Sepolia)

The full end-to-end flow has been run on Sepolia testnet. All values below are real and verifiable on-chain.

### Contract

| Field | Value |
|-------|-------|
| Network | Ethereum Sepolia (chain 11155111) |
| Contract | `0x97d55Ff73f7592F85AafF025a94963d02266cC78` |
| SP1 PLONK gateway | `0xd685a80aF2d1761648e56716af4868d850Dae49B` |
| Program vkey | `0x0098ee1f091411258d9318cb9a146c4e48145cee16b45a774d0445772cbfca4f` |
| SP1 SDK version | `6.1.0` |
| Proof system | PLONK (Gnark BN254) |

### Proof submission

| Field | Value |
|-------|-------|
| Submit tx | [`0xb952c483e839b5cfbe3694ac4e3a3ace9a643d2dea2273d867dd5c5ea8f43ea3`](https://sepolia.etherscan.io/tx/0xb952c483e839b5cfbe3694ac4e3a3ace9a643d2dea2273d867dd5c5ea8f43ea3) |
| Block | 10746772 |
| Event | `SolvencyProven` |

**View the verified proof on Etherscan:**
> https://sepolia.etherscan.io/tx/0xb952c483e839b5cfbe3694ac4e3a3ace9a643d2dea2273d867dd5c5ea8f43ea3

### Attested public values

These are the values committed inside the PLONK proof and verified on-chain. They correspond to a dataset of 100 users and 3 reserves generated by `cargo run -p data-gen`.

| Field | Value |
|-------|-------|
| `merkleRoot` | `0xc62b97ef52f4d1c1139f3d829235bfa7510b43beb1da0bf0d1b2f961452bb41b` |
| `assetsCommitment` | `0xe9f2b15ad68b93aeb0dca75ab6fb017446e547ad0b2725d78405d5dbefb2f23a` |
| `totalLiabilities` | `501,258` |
| `totalAssets` | `601,509` |
| Surplus | `+100,251` (assets exceed liabilities by ~20%) |

`public_values` (128 bytes, Solidity ABI-encoded):
```

```

### Proof bytes

The PLONK proof is 964 bytes. The first 4 bytes (`5a093a2f`) are the SP1 v6.1.0 PLONK verifier selector, used by the gateway to route to the correct versioned verifier contract (`0xc3c6dDDAc8829b233Dc6536Ec024775a57b0AF2A`).

```
0x5a093a2f                        ← verifier selector (SP1 v6.1.0 PLONK)
  0000...0000                     ← exitCode = 0 (success)
  002f850ee998974d...f25352        ← vkRoot (recursion circuit VK)
  0000...0000                     ← nonce
  <864 bytes of Gnark PLONK proof>
```

Full `proof_bytes` are stored in `proof.json` (committed, network mode output) in the repo root.

---

## Prerequisites

| Tool | Purpose | Install |
|------|---------|---------|
| **Rust** (stable) | Build all crates | `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \| sh` |
| **SP1 toolchain** | Compile zkVM program to RISC-V ELF | `curl -L https://sp1up.succinct.xyz \| bash && sp1up` |
| **Foundry** | Compile and test Solidity contracts | `curl -L https://foundry.paradigm.xyz \| bash && foundryup` |
| **Node.js ≥ 18** | Run the Next.js web frontend | [nodejs.org](https://nodejs.org) or via `nvm` |

Verify your SP1 install:
```bash
cargo prove --version   # should print sp1-cli ...
```

---

## Quickstart

### 1. Generate sample data

```bash
cargo run -p data-gen
```

This writes `data/users.json` (100 users) and `data/reserves.json` to the repo root.

You can customise the dataset size:
```bash
cargo run -p data-gen -- --users 500 --surplus 0.2
```

Validation: `--users` and `--reserves` must be ≥ 1; `--surplus` must be ≥ 0 (negative means insolvent).

### 2. Build the zkVM program (ELF)

Run this whenever `crates/program/src/main.rs` changes:

```bash
cd crates/program
cargo prove build
cd ../..
```

The ELF is compiled to `target/elf-compilation/riscv64im-succinct-zkvm-elf/release/solvency-program` and baked into the script binary via `include_bytes!`.

**Note:** if you rebuild the ELF, the program vkey changes automatically — `script/src/main.rs` derives it live from the proving key, and `Deploy.s.sol` reads it from `proof.json` at deploy time. No constants need updating.

### 3. Generate a ZK proof

**Mock mode** (instant, for development — no real proof):
```bash
SP1_PROVER=mock cargo run --manifest-path script/Cargo.toml --bin script
```
Writes to `proof.mock.json` (gitignored). Mock proofs have empty `proof_bytes` and cannot be submitted on-chain.

**Network mode** (real PLONK proof via Succinct Prover Network, ~5–15 min):
```bash
SP1_PROVER=network \
NETWORK_PRIVATE_KEY=<your_succinct_wallet_key> \
cargo run --manifest-path script/Cargo.toml --bin script
```
Writes to `proof.json` (committed). The two output files are intentionally separate so a dev mock run can never overwrite a real proof.

Both files share the same schema:
```json
{
  "proof_bytes":   "0x5a093a2f...",
  "public_values": "0x<merkleRoot_32><assetsCommitment_32><liabilities_32><assets_32>",
  "program_vkey":  "0x0098ee1f091411258d9318cb9a146c4e48145cee16b45a774d0445772cbfca4f"
}
```

`public_values` is 128 bytes (256 hex chars) in Solidity ABI encoding:

| Bytes | Field |
|-------|-------|
| 0–31  | `merkleRoot` — SHA-256 Merkle root of all user balances |
| 32–63 | `assetsCommitment` — SHA-256 hash of all reserve balances |
| 64–95 | `totalLiabilities` — sum of user balances (uint64, 32-byte slot) |
| 96–127 | `totalAssets` — sum of reserve balances (uint64, 32-byte slot) |

### 4. Run Solidity tests

First-time setup — install Solidity dependencies (not tracked in git):
```bash
cd contracts
forge install foundry-rs/forge-std succinctlabs/sp1-contracts
```

Then run tests:
```bash
forge test
```

Expected: **7 tests pass** (4 happy-path + 3 negative/rejection tests).

### 5. Start the web frontend

```bash
cd web
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). The page reads `proof.json` from the repo root and displays the latest attestation. Users can enter any user ID to verify their balance inclusion — Merkle path verification runs in the browser using the Web Crypto API (no Rust binary needed).

If `deployment.json` exists, the UI also shows a live Etherscan link to the `SolvencyProven` transaction.

> **Demo scope note:** The inclusion checker is a demo UX. The server generates Merkle proof material per request and only returns the queried user's data; it is not a production-authenticated lookup service.

---

## Project Structure

```
zk-solvency/
├── crates/
│   ├── types/         # Shared types: UserBalance, ReserveBalance, MerkleTree, assets_commitment
│   ├── data-gen/      # CLI: generates data/users.json and data/reserves.json
│   ├── program/       # SP1 zkVM guest program — the ZK circuit
│   ├── inclusion/     # CLI: Merkle inclusion proof generator and verifier (Rust)
│   └── bench/         # Merkle tree operation benchmarks
├── script/            # Separate Cargo workspace — proof generation via sp1-sdk
│   ├── src/main.rs          # Proof generation script (SP1_PROVER=mock|network)
│   ├── src/bench.rs         # SP1 mock timing benchmark across dataset sizes
│   └── src/test_insolvency.rs  # Verifies guest panic on insolvent inputs → zero public values
├── contracts/
│   ├── src/SolvencyAttestation.sol   # On-chain proof verifier and attestation store
│   ├── script/Deploy.s.sol           # Forge script: deploy SolvencyAttestation
│   ├── script/Submit.s.sol           # Forge script: submit proof on-chain
│   └── test/SolvencyAttestation.t.sol
├── web/
│   ├── app/page.tsx                  # Server component: reads proof.json, shows attestation
│   ├── app/api/verify/route.ts       # POST endpoint: generates one user's Merkle proof server-side
│   ├── _components/InclusionChecker.tsx  # Client component: submits ID, re-derives leaf, verifies proof in browser
│   └── _lib/merkle.ts                # TypeScript Merkle tree (mirrors crates/types/src/merkle.rs)
├── docs/              # Report and presentation (PDF)
├── proof.json         # Latest proof artifacts (proof_bytes, public_values, program_vkey)
├── deployment.json    # Sepolia deployment info (contract address, tx hash)
└── data/              # Generated (gitignored): users.json, reserves.json
```

**Why is `script/` a separate Cargo workspace?** `sp1-sdk` carries a forked `serde_core` that conflicts with `alloy`'s derive macros in a shared dependency graph. The separate workspace is the upstream-recommended pattern for SP1 projects.

---

## Cryptographic Details

### Merkle tree

- **Leaf**: `SHA-256(user.id_le_8 || user.balance_le_8)` — 16-byte input, little-endian u64s
- **Node**: `SHA-256(left_32 || right_32)`
- **Padding**: repeated last leaf to next power of two (e.g. 100 users → 128 leaves → depth 7)
- **Inclusion proof**: sibling path from leaf to root, O(log N)

### Assets commitment

`SHA-256(reserve[0].id_le_8 || reserve[0].balance_le_8 || reserve[1].id_le_8 || reserve[1].balance_le_8 || ...)` — binds the prover to the exact asset set (identity + amount) in order. Two reserve sets that sum to the same total but differ in individual allocations or IDs produce different commitments.

### Public value encoding

Solidity ABI encoding (`abi.encode(bytes32, bytes32, uint64, uint64)`): each field occupies a 32-byte slot. `uint64` values are right-aligned with leading zero padding.

### Program verification key

The program vkey is a hash of the compiled zkVM circuit (ELF). It is deterministic for a given ELF. `script/src/main.rs` derives it at runtime from `pk.vk.bytes32()` and writes it into `proof.json`. `Deploy.s.sol` reads it back via `PROGRAM_VKEY=$(jq -r '.program_vkey' proof.json)`, so the deployed contract always stores the vkey that matches the proof — no constants to keep in sync.

Current vkey (for the ELF in this repo): `0x0098ee1f091411258d9318cb9a146c4e48145cee16b45a774d0445772cbfca4f`

---

## Running Benchmarks

**Merkle tree operations** (native Rust, release build):
```bash
cargo run -p bench --release
```

**SP1 proof generation timing** across dataset sizes:
```bash
SP1_PROVER=mock cargo run --manifest-path script/Cargo.toml --bin bench
```

### Reference numbers (Apple Silicon, SP1 v6.1.0)

**SP1 mock execution time** — full zkVM execution without generating a real proof:

| N (users) | SP1 Mock Time (s) | Gas Cost (submitProof) |
|-----------|-------------------|------------------------|
| 10 | 0.074 | ~250k (constant) |
| 100 | 0.469 | ~250k (constant) |
| 500 | 1.947 | ~250k (constant) |
| 1,000 | 4.031 | ~250k (constant) |
| 5,000 | 27.152 | ~250k (constant) |

Gas cost is constant regardless of N because the PLONK verifier always processes a fixed 964-byte proof.

**Merkle tree operations** (release build, averaged over multiple iterations):

| N (users) | build (ms) | prove (µs) | verify (µs) |
|-----------|------------|------------|-------------|
| 10 | 0.016 | 0.121 | 2.575 |
| 100 | 0.088 | 0.137 | 3.124 |
| 500 | 0.271 | 0.167 | 3.073 |
| 1,000 | 0.458 | 0.142 | 2.905 |
| 5,000 | 3.116 | 0.149 | 3.780 |

`build` is O(N) (dominated by N SHA-256 leaf hashes); `prove`/`verify` are O(log N) and effectively constant. Tree depth is ⌈log₂(N)⌉ after padding — e.g. N=100 → 128 leaves → depth 7.

---

## Deployment

These are the exact steps used to deploy and verify on Sepolia testnet.

### Prerequisites

- Sepolia ETH for gas (two transactions: deploy + submit). Get from [Google Cloud Web3 Faucet](https://cloud.google.com/application/web3/faucet/ethereum/sepolia).
- A Succinct Prover Network account with PROVE tokens for network proof generation. Deposit ETH at [network.succinct.xyz](https://network.succinct.xyz).
- A Sepolia RPC URL (e.g. from [Alchemy](https://alchemy.com)).

Store secrets in `.env` (gitignored):
```bash
export NETWORK_PRIVATE_KEY=0x<your_wallet_private_key>
export SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/<your_key>
```

### Step 1 — Generate the proof

```bash
source .env
SP1_PROVER=network \
NETWORK_PRIVATE_KEY=$NETWORK_PRIVATE_KEY \
cargo run --manifest-path script/Cargo.toml --bin script
```

This writes `proof.json` with `proof_bytes`, `public_values`, and `program_vkey`. The vkey is derived live from `pk.vk.bytes32()` — it is never hardcoded in source.

### Step 2 — Deploy the contract

```bash
source .env
PRIVATE_KEY=$NETWORK_PRIVATE_KEY \
PROGRAM_VKEY=$(jq -r '.program_vkey' proof.json) \
forge script contracts/script/Deploy.s.sol:Deploy \
  --root contracts --rpc-url $SEPOLIA_RPC_URL --broadcast
```

`PROGRAM_VKEY` is read from `proof.json` at deploy time, so the contract always stores exactly the vkey that matches the proof. The script also prints the vkey it used, so you can cross-check.

Copy the printed contract address. The script deploys `SolvencyAttestation` with the SP1 PLONK verifier gateway and the program vkey baked in as immutables.

**Actual deployment (this repo):**
- Contract: `0x97d55Ff73f7592F85AafF025a94963d02266cC78`
- SP1 PLONK gateway: `0xd685a80aF2d1761648e56716af4868d850Dae49B`
- Program vkey: `0x0098ee1f091411258d9318cb9a146c4e48145cee16b45a774d0445772cbfca4f`

### Step 3 — Submit the proof

```bash
source .env
cd ..   # back to repo root
CONTRACT_ADDRESS=0x97d55Ff73f7592F85AafF025a94963d02266cC78 \
PRIVATE_KEY=$NETWORK_PRIVATE_KEY \
PROOF_BYTES=$(python3 -c "import json; print(json.load(open('proof.json'))['proof_bytes'])") \
PUBLIC_VALUES=$(python3 -c "import json; print(json.load(open('proof.json'))['public_values'])") \
forge script contracts/script/Submit.s.sol:Submit \
  --root contracts --rpc-url $SEPOLIA_RPC_URL --broadcast
```

On success the contract emits `SolvencyProven` and stores the attestation on-chain.

**Actual submission (this repo):**
- Tx hash: `0xb952c483e839b5cfbe3694ac4e3a3ace9a643d2dea2273d867dd5c5ea8f43ea3`
- Block: 10746772
- Etherscan: https://sepolia.etherscan.io/tx/0xb952c483e839b5cfbe3694ac4e3a3ace9a643d2dea2273d867dd5c5ea8f43ea3

### Step 4 — Save deployment info

Create `deployment.json` in the repo root so the web UI shows the Etherscan link:

```json
{
  "network":          "sepolia",
  "contract_address": "0x97d55Ff73f7592F85AafF025a94963d02266cC78",
  "submit_tx_hash":   "0xb952c483e839b5cfbe3694ac4e3a3ace9a643d2dea2273d867dd5c5ea8f43ea3",
  "program_vkey":     "0x0098ee1f091411258d9318cb9a146c4e48145cee16b45a774d0445772cbfca4f",
  "sp1_verifier":     "0xd685a80aF2d1761648e56716af4868d850Dae49B"
}
```

The web UI reads this file at build time and renders a clickable Etherscan link on the attestation card.

---

## Troubleshooting

### `InvalidProof` on `submitProof`

The most common cause is a `programVKey` mismatch: the vkey stored in the contract doesn't match the one the proof was generated for. This can happen if the ELF was rebuilt between proving and deploying, or if the deploy script was run with a stale vkey.

With the current setup this should not happen — `script/src/main.rs` derives the vkey live and writes it to `proof.json`, and `Deploy.s.sol` reads it from there. If you see `InvalidProof`, verify the vkey in `deployment.json` matches `proof.json`, and if not, redeploy the contract (the existing proof is still valid).

### SP1 PLONK vs Groth16

This project uses PLONK (`sp1-sdk v6.1.0`). The SP1 v6.1.0 Groth16 on-chain verifier has a known circuit bug; PLONK is unaffected. The PLONK proof selector `0x5a093a2f` routes through the gateway to `V6_1_0_SP1_VERIFIER_PLONK` at `0xc3c6dDDAc8829b233Dc6536Ec024775a57b0AF2A`.

### Wrong working directory for `forge script`

Run `forge script` either from `contracts/` or pass `--root contracts` from the repo root. Running from the repo root without `--root contracts` causes Forge to not find `foundry.toml` and fail with file-not-found errors.
