# Rust build artifacts
/target/
**/*.rs.bk

# Generated data files
/data/

# Foundry build artifacts and cache
contracts/out/
contracts/cache/

# Foundry broadcast logs (per-run deployment records — key facts are in deployment.json)
contracts/broadcast/

# Foundry dependencies — install with: cd contracts && forge install foundry-rs/forge-std succinctlabs/sp1-contracts
contracts/lib/

# macOS
.DS_Store

# Environment / secrets
.env

# Claude Code local settings
.claude

script/target/
script/Cargo.lock

# Mock proof artifacts (dev only — not verifiable on-chain)
proof.mock.json

# Next.js frontend
web/node_modules/
web/.next/